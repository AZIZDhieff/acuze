{
  "name": "your-app-name",
  "version": "1.0.0",
  "description": "A description of your MERN stack application",
  "main": "server.js",
  "scripts": {
    "start": "set MONGODB_URI=your_mongodb_connection_string && set PORT=your_port && node server.js",
    "build": "cd client && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Your Name",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^8.6.0",
    "express": "^4.19.2",
    "mongoose": "^5.13.22"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
